{
  "name": "ozon-assist-plugin",
  "version": "1.0.1",
  "description": "Ozon Assist Chrome Extension in TypeScript",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "rm -rf dist && vite build",
    "watch": "vite build --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.262",
    "typescript": "^5.3.3",
    "vite": "^5.1.4",
    "vite-plugin-static-copy": "^1.0.1"
  }
}
